# Fe-ManagementSystem (React + Vite)


## Chạy dự án

1. Cài đặt phụ thuộc:

```bash
npm install
```

2. Chạy môi trường phát triển:

```bash
npm run dev
```

3. Mở URL do Vite cung cấp (ví dụ: http://localhost:5173).
